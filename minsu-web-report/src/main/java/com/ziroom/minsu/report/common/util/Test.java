package com.ziroom.minsu.report.common.util;

import com.ziroom.minsu.report.order.entity.OrderEntity;

public class Test {

	public static void main(String []args){
		OrderEntity oe = new OrderEntity();
		 /*Field[] fields = oe.getClass().getDeclaredFields();
		 for(Field field : fields){
			 field.setAccessible(true);
			 FieldMeta meta = field.getAnnotation(FieldMeta.class); 
			 if (field.isAnnotationPrelsent(NotNull.class)) {
			      //这样就获取到这个注解属性了
		      NotNull d = fd.getAnnotation(NotNull.class);
		     }
	         System.out.println("ttttttttttttttt="+field.getAnnotation(NotNull.class).message());
		 }*/
		//Field field = 
	}
	
}
